Default `false` accepts the partial-ISM-1175 implementation.

When `true`, applies `S-1-5-114` to `SeDenyNetworkLogonRight` and `SeDenyRemoteInteractiveLogonRight`. Will break WinRM-based remote management using local admin accounts. 